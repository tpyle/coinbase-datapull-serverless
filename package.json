{
  "name": "datapull-serverless",
  "version": "1.0.0",
  "description": "Pulls data from coinbase's crypto currency price API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -f build.zip",
    "build": "zip build.zip * -r",
    "re": "npm run clean && npm run build"
  },
  "author": "Thomas Pyle",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "axios": "^0.21.0",
    "config": "^3.3.2",
    "mongodb": "^3.6.2"
  }
}
